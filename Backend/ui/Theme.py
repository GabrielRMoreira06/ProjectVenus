"""
Theme.py

Shared color palette for every Qt window (InputWindow, PanelWindow,
MessageComposer). Centralized so restyling one thing consistently
restyles all of them — previously InputWindow had its own gray theme
and PanelWindow had its own pink one, quietly drifting apart.
"""

PINK = "#ff2fb0"
PINK_SOFT = "#ff7fce"
BG_DARK = "#0a0007"
BG_PANEL = "#170a14"
BG_BUBBLE = "#1f0f1c"
